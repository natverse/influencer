# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

name: test-coverage

permissions: read-all

jobs:
  test-coverage:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::covr, any::xml2
          needs: coverage

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libharfbuzz-dev libfribidi-dev

      # Skip Python for coverage - focus on R code coverage
      - name: Set environment for R-only testing
        run: |
          echo "R_INFLUENCER_SKIP_PYTHON=true" >> $GITHUB_ENV

      - name: Test coverage
        run: |
          cov <- covr::package_coverage(
            quiet = FALSE,
            clean = FALSE,
            install_path = file.path(normalizePath(Sys.getenv("RUNNER_TEMP"), winslash = "/"), "package")
          )
          covr::to_cobertura(cov)
        shell: Rscript {0}

      # Codecov upload disabled - uncomment and add CODECOV_TOKEN secret to enable
      # - uses: codecov/codecov-action@v4
      #   with:
      #     fail_ci_if_error: ${{ github.event_name != 'pull_request' && true || false }}
      #     file: ./cobertura.xml
      #     plugin: noop
      #     disable_search: true
      #     token: ${{ secrets.CODECOV_TOKEN }}
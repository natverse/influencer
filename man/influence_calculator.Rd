% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/influence-calculator-r.R
\name{influence_calculator}
\alias{influence_calculator}
\title{Unified Interface for Influence Calculation}
\usage{
influence_calculator(
  edgelist_simple = NULL,
  meta = NULL,
  filename = NULL,
  sqlite = FALSE,
  method = "r",
  signed = FALSE,
  count_thresh = 3,
  const = 24
)
}
\arguments{
\item{edgelist_simple}{Data frame with edge list containing columns 'pre', 'post',
'count', and 'norm'. Required when sqlite = FALSE.}

\item{meta}{Data frame with metadata containing at least 'root_id' column.
Required when sqlite = FALSE.}

\item{filename}{Character. Path to SQLite database file. Used when sqlite = TRUE.}

\item{sqlite}{Logical. Whether to load data from SQLite database (default: FALSE).}

\item{method}{Character. Either "python" or "r" (default: "r").}

\item{signed}{Logical. Whether to use signed connectivity matrix (default: FALSE).}

\item{count_thresh}{Numeric. Minimum threshold count (default: 3).}

\item{const}{Numeric. Constant value to add to log influence scores in adjusted
influence calculation (default: 24). Only used for R implementation.}
}
\value{
InfluenceCalculator object (either Python or R implementation)
}
\description{
Provides a unified interface that can use either the Python or R implementation
of influence calculation.
}
\examples{
\dontrun{
# Use R implementation with data frames (recommended)
ic.df <- influence_calculator(edgelist_simple = my_edges, meta = my_meta)

# Use R implementation with SQLite database
ic.sqlite <- influence_calculator(filename = "connectome.sqlite", sqlite = TRUE)

# Use Python implementation (requires SQLite database)
ic.py <- influence_calculator(filename = "connectome.sqlite", sqlite = TRUE, method = "python")
}
}
